Real market data is intentionally NOT included.

When executed in a network-enabled environment:
1. verify legitimate NSE sources;
2. download incrementally;
3. preserve raw files;
4. validate;
5. record checksums in the manifest;
6. only then run the production VIKRAM engine.

Synthetic fixtures, if added, must remain clearly marked `SYNTHETIC_TEST_ONLY`.
