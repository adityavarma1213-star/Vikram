window.VIKRAM_RULE_ENGINE={compare(a,op,b){if(a==null||b==null||!Number.isFinite(Number(a))||!Number.isFinite(Number(b)))return null;a=Number(a);b=Number(b);return{'>':a>b,'>=':a>=b,'<':a<b,'<=':a<=b,'==':a===b,'!=':a!==b}[op]??null;},evalCondition(c,row){const a=c.left==='score'?row.score:row[c.left];return{result:this.compare(a,c.operator,c.right),reason:`${c.left} ${c.operator} ${c.right}`};},eval(rule,row){if(rule.type==='condition')return this.evalCondition(rule,row);const rs=(rule.rules||[]).map(r=>this.eval(r,row));const known=rs.filter(x=>x.result!==null);if(!known.length)return{result:null,reason:'N/A'};let result=rule.operator==='OR'?known.some(x=>x.result):known.every(x=>x.result);if(rule.operator==='NOT')result=!known[0].result;return{result,reason:rs.map(x=>x.reason).join(` ${rule.operator} `)};}};
