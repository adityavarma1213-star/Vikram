document.addEventListener('DOMContentLoaded',()=>{const input=document.getElementById('stockName'),button=document.getElementById('btnAnalyze'),box=document.getElementById('searchSuggestions');if(!input||!button)return;const run=()=>{const q=input.value.trim().toUpperCase();if(!q)return;window.dispatchEvent(new CustomEvent('vikram:analyze',{detail:{ticker:q}}));};button.addEventListener('click',run);input.addEventListener('keydown',e=>{if(e.key==='Enter')run();});input.addEventListener('input',()=>{if(!box||!window.VIKRAM_ENGINE)return;const q=input.value.trim();if(!q){box.classList.add('hidden');box.innerHTML='';return;}const rows=window.VIKRAM_ENGINE.searchSuggestions(q).slice(0,8);box.innerHTML=rows.map(c=>`<button type="button" class="suggestion-item" data-ticker="${c.ticker}">${c.name} <span>${c.ticker}</span></button>`).join('');box.classList.toggle('hidden',rows.length===0);box.querySelectorAll('[data-ticker]').forEach(b=>b.onclick=()=>{input.value=b.dataset.ticker;box.classList.add('hidden');run();});});});
